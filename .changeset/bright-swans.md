---
"poe-idol-planner": minor
---

Add duplicate detection for shared idol sets. When importing a shared set that already exists in your collection (based on content hash), you'll see a prompt to either view the existing set or import it anyway.
